<?php

/**
 * @file
 */


/**
 * Implements hook_theme().
 */
function trustpilot_carousel_theme($existing, $type, $theme, $path) {
  return [
    'trustpilot_reviews' => [  // note underscore, be consistent everywhere
      'variables' => [
        'reviews' => [],
      ],
      'template' => 'trustpilot-reviews', // trustpilot-reviews.html.twig file expected
    ],
  ];
}
